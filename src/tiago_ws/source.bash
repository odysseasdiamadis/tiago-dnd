#!/usr/bin/bash

# cd ..     # do this before any other commands in this script (after you start the docker)

source /tiago_public_ws/devel/setup.bash
source /src/tiago_ws/devel/setup.bash
# source ./src/install-deps.sh        # this install libraries and sources the venv env in the current window